# Java format-plugin SDK

Small JVM-facing interface for contributing file/application structure evidence without coupling plugins to the C++ solver. v0.1 includes signature-level probes only; plugins return evidence/constraints, never reconstructed bytes.
